'use strict';

/**
 * point-ladder-exam service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::point-ladder-exam.point-ladder-exam');
